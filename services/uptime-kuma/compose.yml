services:
    uptime-kuma:
        image: louislam/uptime-kuma:latest
        restart: unless-stopped
        ports:
            - "3001:3001"
        volumes:
            - data:/app/data
        extra_hosts:
            - "host.docker.internal:host-gateway"

volumes:
    data:
